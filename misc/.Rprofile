
## local library
# dir_lib_local = "~/R/x86_64-redhat-linux-gnu-library/3.1"
# .libPaths(c( .libPaths(), dir_lib_local) )
# rm(dir_lib_local)

## Global Options

# options(stringsAsFactors=FALSE)
# options(row.names=FALSE)
# options(digits=3)

myop = list(stringsAsFactors=FALSE,
            row.names=FALSE,
            digits=3)

message('Note: custom global options used')
print(myop)

options(myop)
rm(myop)

# getOption("stringsAsFactors")
# getOption("row.names")



## Packages
# options(repos = "http://cran.rstudio.com")
# options(repos = "https://cran.rstudio.com")

# devtools::install_github('mikejacktzen/datzen',force=TRUE)
library(datzen)
# install.packages('dplyr',repos="https://cran.rstudio.com")
library(dplyr)

## journal article templates
## https://github.com/rstudio/rticles
# devtools::install_github("rstudio/rticles") 

